
//Ersetzt die Mitfahrerwerte durch IDs aus der Datenbank
export const rideRequestsToIDs= (driveData) =>{
   

    

    //return newDriveData
}

//Ersetzt die MitfahrerIDs durch die Mitfahrer aus der Datenbank
export const rideRequestsToData= (driveData) =>{
    

    //return newDriveData
}

//Überschreibt eine Fahrt in der Datenbank mit einer neuen, geänderten Fahrt
export const overrideDriveDataInDB = (driveData) =>{


}

//Überschreibt den User in der Datenbank mit dem neuen, geänderten Fahrer
export const overrideUserDataInDB = (userData) => {

}